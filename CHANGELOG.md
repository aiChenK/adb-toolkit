# 更新日志 (CHANGELOG)

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 规范，版本号遵循 [语义化版本 2.0.0](https://semver.org/lang/zh-CN/)。

---

## [1.0.1] - 2026-08-31

### ✨ 新增特性 (Added)
- **设备配置本地持久化**：设备分组参数（IP、端口、包名、代理地址、分组名称）自动保存至浏览器 `localStorage`，页面刷新后配置不丢失。
- **设备组重命名**：折叠面板标题栏与操作栏增加重命名功能，支持免展开快速修改分组名称并实时持久化。
- **一键重置配置**：顶部操作栏下拉菜单新增「重置设备组配置」功能，支持确认后一键清空本地缓存并恢复为默认初始状态。

### ⚡ 优化与改进 (Changed)
- **折叠面板头部展示**：优化设备面板 Header 布局与样式，标题和 IP 地址徽章展示更清晰，操作体验更流畅。

---

## [1.0.0] - 2026-08-31

### 🚀 架构重构 (Major)
- **单二进制架构**：将原前后端独立仓库（`adb-service` 与 `adb-web`）合并重构为 `adb-toolkit` 单体工程，通过 Go `embed` 内嵌 Vue 3 前端静态产物，实现零依赖、绿色免安装、双击运行并自动唤起浏览器。
- **构建中心化**：引入标准化 `Makefile` 与 `build.sh`，支持一键前后端联合开发（`make dev`）与多平台交叉编译（macOS arm64/amd64、Linux amd64、Windows amd64）。
- **仓库精简瘦身**：彻底移除废弃的 Docker 镜像配置及历史残留大二进制文件，使 Git 仓库轻量规范。

### ✨ 新增特性 (Added)
- **设备自动扫描**：新增已连接 Android 设备扫描接口（`GET /devices`）与前端卡片快捷填充功能。
- **常用预置命令**：控制台提供常用快捷预置命令选择（如查看在线设备、定向断开、查看第三方包名、查看电池状态等）。
- **日志快速复制**：控制台输出流支持单条执行结果一键复制至剪贴板。
- **协作规范**：新增 `AGENTS.md` 规范化开发者与 AI 协作准则。

### ⚡ 优化与改进 (Changed)
- **命令解析与健壮性**：增强 ADB 命令参数切片解析能力，精确支持带引号复杂参数，并完整捕获 stdout 与 stderr 错误信息。
- **智能 IP 识别**：重构跨平台物理网卡 IPv4 过滤与排序算法，精准识别 macOS / Linux / Windows 真实局域网 IP。
- **定向断开保护**：设备异常处理由全局 `disconnect` 改为目标定向断开，避免影响其他连接设备。
- **前端体验升级**：前端代码结构模块化（Axios API 解耦、Ant Design Vue 4.x 组件按需适配）。

---
